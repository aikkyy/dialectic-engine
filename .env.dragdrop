# Loaded automatically when Vite is started with `--mode dragdrop`.
# Switches /form/:category from the new freeform input form back to the
# original orbit + drag-and-drop form (CategoryDragAndDrop.vue).
VITE_FORM_VARIANT=dragdrop
